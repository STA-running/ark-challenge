# 2026-06-04 工作日志

## 修复：焰狐龙梓兰干员头像不显示

**根因**：SquadPage.tsx 中 `PROFESSION_ICONS` 的 8 个职业图标 URL 全部 404（PRTS Wiki 文件结构变更），导致：
1. 任何头像加载失败后 fallback 到职业图标也 404 → 无限 error 循环 → 裂图
2. 所有直接使用职业图标作为默认图标的干员也受影响

**修复内容**：
- `src/pages/SquadPage.tsx`：将 PROFESSION_ICONS 从远程 PNG URL 替换为内联 SVG data URI（`makeProfIcon()` 函数），彻底消除外部依赖
- `src/pages/SquadPage.tsx`：3 个 `onError` handler 增加 `dataset.fallback` 防无限循环保护

## 修复：Vite 缓存导致 UI 布局不对

**根因**：`node_modules/.vite` 依赖预构建缓存残留旧版本 Tailwind CSS

**修复内容**：
- 删除 `node_modules/.vite` 和 `dist/` 后重新构建
- 新鲜构建的 CSS 从 14.38KB 降为 14.11KB，产物哈希变化

## Git 备份教学

给用户讲解了：
- Git push 不丢代码的原理（快照链 + GitHub 冗余存储）
- 版本回滚的 4 种场景：checkout . / reset --soft / reset --hard / revert
- 项目备份的 3 种方法：git push / ZIP 打包 / tar 命令行

## QA 测试 + BugFix：十连抽卡功能

**QA 测试报告**：`test-report.md` — 22项通过，2严重Bug，2中等Bug，2轻微问题

**修复的 Bug**：
1. 🔴 抽卡池受手动筛选污染 → 移除 gachaPool 中的 professionFilter/rarityFilter/positionFilter/raceFilter
2. 🔴 锁定区双击加入编队跳过约束检查 → 增加 constrainedInvalid 判断，违规干员灰色+⚠标记+禁双击
3. 🟡 ringSpin CSS 动画缺失 → index.css 添加 @keyframes ringSpin
4. 🟡 概率弹窗未标注6★边界 → 添加警告文字
5. 🔵 重新选择未清空标签选中态 → 添加 setSelectedTagIds([]); setRandomTags([])

## 新功能：卡池干员黑名单

**需求**：设置页面增加黑名单，拉黑的干员不出现在十连抽卡中，但手动模式不受影响。

**实现**：
- `src/stores/appStore.ts`：新增 `gachaBlacklist` 状态 + `toggleGachaBlacklist`/`clearGachaBlacklist`/`loadGachaBlacklist` 操作，localStorage 持久化
- `src/pages/SettingsPage.tsx`：新增「🎰 卡池干员黑名单」分区，支持按职业/星级筛选干员列表，点击拉黑/取消，已拉黑干员以红色标签展示（名称+星级）
- `src/pages/SquadPage.tsx`：`gachaPool` 排除黑名单干员 + 启动时加载黑名单

## 移植：十连抽卡+黑名单→手机端UI

**改动文件**：`src/pages/MobileSquadPage.tsx`（+约250行）

**移植内容**：
- Tab1编队：新增「✋ 手动选人」/「🎰 十连抽卡」模式切换 + 完整抽卡UI（DRIVE按钮、结果展示含翻转动画、锁定/重抽、已锁定干员加入编队约束检查）
- Tab2设置：新增「🎰 卡池干员黑名单」管理（按职业/星级筛选、已拉黑列表、全部恢复）
- 抽卡池计算排除黑名单干员 + 启动加载黑名单
- 抽卡说明弹窗（概率表 + 使用方式 + 注意事项）

## 全量回归测试 + 约束校验审查

**测试结果**：80项引擎测试全通过 | TypeScript编译 ✅ | Vite构建 ✅

**发现并修复 11 个 Bug**：
1. 🔴 分享码仅支持29标签 → v2升级到10字节/40标签，保留v1兼容
2. 🔴 allowedOperators/bannedOperators约束从未生效 → filterOperatorsByHardConstraints添加检查
3. 🔴 allowedSubProfessions/bannedSubProfessions从未生效 → 同上
4. 🔴 validator缺失4项约束检查 → 补充allowedOperators/bannedOperators/allowedSubProfessions/bannedSubProfessions
5. 🔴 validator truthy/null检查不一致 → 统一为`!== null`
6. 🔴 rareRaceOnly编队级约束从未检查 → validateSquad添加
7. 🔴 getDisabledReason缺失5项约束解释 → 补充完整
8. 🔴 手机端_reason恒为空字符串 → MobileSquadPage添加getDisabledReason调用
9. 🔴 appStore gachaBlacklist重复定义 → 删除重复声明
10. 🟡 测试预期值过时 → 更新为动态期望
11. 🟡 测试中stage ID不存在 → 修正为实际关卡ID

**修改文件**：shareCode.ts, tagEngine.ts, validator.ts, SquadPage.tsx, MobileSquadPage.tsx, appStore.ts, test_engine.mjs
